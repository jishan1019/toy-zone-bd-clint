import React from "react";
import useDynamicTitle from "../CustomHook/useDynamicTitle";

const AddAToy = () => {
  useDynamicTitle("Toy Zone | Add A Toy");

  return <div>add a toy</div>;
};

export default AddAToy;
